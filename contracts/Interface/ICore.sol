// // SPDX-License-Identifier: MIT
// pragma solidity ^0.8.9;
// interface ICore {
//     function transferBalance(uint amount, address token, address to, address sender) external;

//     function addNeworacle(address token, address _oracle) external;

//     function updateBalanceInUSD(address sender) external;

//     function deposit(uint amount, address token, address sender) external;

//     function withdrawal(uint amount, address token, address oracle, address sender) external;

//     function balanceTokenAccount(address tokenAddress, address oracle, address sender) external view returns(uint, uint);

// }
